# Dependency management moved to pyproject.toml.
#
# Install this repository with:
#   uv sync
#
# The uv configuration uses the Tsinghua PyPI mirror for ordinary packages and
# the official PyTorch CUDA 12.8 wheel index for torch and torchvision.
